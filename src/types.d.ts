export interface CV {
  id: string;
  cv_link: string;
}

export interface PersonalInfo {
  id: string;
  cv_link: string;
  university: string;
}

export interface Languaje {
  id: string;
  name: string;
}

export interface Project {
  id: string;
  title: string;
  imageUrl: string;
}
